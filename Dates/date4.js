// JavaScript does not have built-in date formatting, but you can use methods like toDateString(), toISOString(),
//  or libraries like date-fns or moment.js:

let localeString = now.toLocaleDateString('en-US');
let isoString = now.toISOString();
let dateString = now.toString();
