// Java Script is synchronous single-thrended Language
// Every thing is happen inside Exexution Context
//  it have several phases based on lines and functions
// When a Function is calling then thre cretaed a new execution context inside the Global Execution Context
//  that time the GEC(Global Execution Context) will put on call stack
// JavaScript uses a single call stack to manage execution contexts.
// Stack Creation: When a function is called, its execution context is created and pushed onto the stack.